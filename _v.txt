=================================================================
  TRUE AI vs AI ? ONE EPISODE, BOTH MINDS EVERY TURN
  (LLM attacker prompt + LLM defender prompt per turn)
=================================================================

TURN  [ATTACKER LLM DECISION]        reward  [DEFENDER LLM DECISION]
------------------------------------------------------------------------------
1     scan(1) -> Scanned Web Server: vuln=0 +0.05   monitor(3) -> Defender: Monitoring Datab
2     exploit(1) -> ? Exploit succeeded on Web +0.35   patch(2) -> Defender: Patched App Serv
3     lateral_move(1) -> Web Server not compromised -0.02   isolate(3) -> Defender: Isolated Databas
4     scan(2) -> Node 2 not reachable from  -0.02   restore(1) -> Defender: restore failed ?
5     exploit(2) -> App Server not adjacent to -0.02   block(2) -> Defender: Blocked App Serv
6     lateral_move(2) -> App Server not adjacent. -0.02   monitor(1) -> Defender: Monitoring Web S
7     exploit(3) -> Database not adjacent to U -0.02   patch(3) -> Defender: Patched Database
8     exfiltrate(3) -> Database not compromised.  -0.05   isolate(2) -> Defender: Isolated App Ser

=================================================================
  HOW IT WORKS:
  - Both decisions happen in the SAME turn, SAME episode
  - LLM has ATTACKER system prompt for call 1
  - LLM has DEFENDER system prompt for call 2
  - Neither knows what the other will do next turn
  - This is equivalent to two independent agents battling
=================================================================
